============
Installation
============

At the command line::

    $ easy_install django-url-shorter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-url-shorter
    $ pip install django-url-shorter
